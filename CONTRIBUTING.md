# Contributing

Keep changes inside the OCPP 1.6 JSON application layer. Do not add a
WebSocket transport in this repository. Run `moon check --target wasm-gc --deny-warn`
and `moon test --target wasm-gc --deny-warn` before sending a patch.
